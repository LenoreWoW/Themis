const fs = require('fs'); const filePath = 'themis-client-final/src/pages/ProjectDetailPage.tsx'; fs.readFile(filePath, 'utf8', (err, data) => { if (err) { console.error('Error reading file:', err); process.exit(1); } const fixedData = data.replace('          }        } catch (err) {', '          }\n        } catch (err) {'); fs.writeFile(filePath, fixedData, 'utf8', (err) => { if (err) { console.error('Error writing file:', err); process.exit(1); } console.log('Successfully fixed the formatting issue'); }); });
